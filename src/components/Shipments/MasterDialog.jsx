// Removed in Shipments v2 (7/2/26) — the inbound-master concept is gone; tracking
// is a plain field set from MarkShippedDialog. Stub kept so stale imports fail loudly.
export default function MasterDialog() {
  return null;
}
